apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: test-app
    env: dev
    managed-by: idp-cli
  name: test-app
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-app
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: test-app
        env: dev
    spec:
      containers:
      - image: nginx:alpine
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        name: test-app
        ports:
        - containerPort: 80
        readinessProbe:
          failureThreshold: 2
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          limits:
            cpu: 200m
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 64Mi
      serviceAccountName: deployer
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: test-app
    env: dev
    managed-by: idp-cli
  name: test-app
  namespace: dev
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: test-app
  type: ClusterIP
